# Core API
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# Database (MySQL 8.0)
SQLAlchemy>=2.0,<3.0
PyMySQL>=1.1,<2.0
# MySQL 8.0 defaults to caching_sha2_password. PyMySQL needs cryptography for
# the RSA key exchange that full (cold-cache) auth performs over a plaintext
# connection — without it the FIRST connect after a server restart or a
# FLUSH PRIVILEGES dies with "'cryptography' package is required", while a
# warm auth cache hides the problem entirely.
cryptography>=42

# Data / ML
pandas>=2.2,<3.0
numpy>=1.26,<3.0
scikit-learn>=1.5,<2.0
xgboost>=2.0,<3.0
lightgbm>=4.3,<5.0
joblib>=1.4,<2.0
matplotlib>=3.8,<4.0        # confusion matrix figures only

# GAN stage only (imported lazily inside training/train_gan.py)
torch>=2.2,<3.0

# Chatbot (LLM) — imported lazily inside chat/engine.py
anthropic>=0.40,<1.0

# Tests
pytest>=8.2,<9.0
httpx>=0.27,<1.0            # FastAPI TestClient dependency
